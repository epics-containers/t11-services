#! Generated by VisualDCT v2.6
#! DBDSTART
#! DBD("../../dbd/lakeshore331.dbd")
#! DBDEND


################################################################
#
# Lakeshore 331 Temperature Controller template file.
#
# Macros:
#   P - Prefix for PV name
#   PORT - Bus/Port Address (eg. ASYN Port).
#   ADDR - Address on the bus (optional)
#   TEMPSCAN - SCAN rate for the temperature/voltage readings
#   SCAN - SCAN rate for non-temperature/voltage parameters.
#   ONOFFDEFAULT - Default value for ON/OFF
#
# Notes: The temperatures in Kelvin are archived once every 10 secs.
#
################################################################
################################################################
# Read records
################################################################
# /// 
# /// Read the ID string from the device.
# ///
record(stringin, "$(P):ID") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getID $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the heater output value.
# ///
record(ai, "$(P):HTR") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getHTR $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(PREC, "3")
  field(EGU, "%")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the setpoint temperature.
# ///
record(ai, "$(P):SETP") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getSETP $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(PREC, "3")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the temperature on channel 0 in Kelvin.
# /// This is archived every 10 seconds.
# ///
#% archiver 10 Monitor
#% alh
record(ai, "$(P):KRDG0") {
  field(DTYP, "stream")
  field(DESC, "Channel 0 temperature")
  field(INP, "@lakeshore331.protocol getKRDG(0) $(PORT) $(ADDR)")
  field(SCAN, "$(TEMPSCAN) second")
  field(PREC, "3")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the temperature on channel 1 in Kelvin.
# /// This is archived every 10 seconds.
# ///
#% archiver 10 Monitor
#% alh
record(ai, "$(P):KRDG1") {
  field(DTYP, "stream")
  field(DESC, "Channel 1 temperature")
  field(INP, "@lakeshore331.protocol getKRDG(1) $(PORT) $(ADDR)")
  field(SCAN, "$(TEMPSCAN) second")
  field(PREC, "3")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the temperature on channel 2 in Kelvin.
# /// This is archived every 10 seconds.
# ///
#% archiver 10 Monitor
#% alh
record(ai, "$(P):KRDG2") {
  field(DTYP, "stream")
  field(DESC, "Channel 2 temperature")
  field(INP, "@lakeshore331.protocol getKRDG(2) $(PORT) $(ADDR)")
  field(SCAN, "$(TEMPSCAN) second")
  field(PREC, "3")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the temperature on channel 3 in Kelvin.
# /// This is archived every 10 seconds.
# ///
#% archiver 10 Monitor
#% alh
record(ai, "$(P):KRDG3") {
  field(DTYP, "stream")
  field(DESC, "Channel 3 temperature")
  field(INP, "@lakeshore331.protocol getKRDG(3) $(PORT) $(ADDR)")
  field(SCAN, "$(TEMPSCAN) second")
  field(PREC, "3")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the raw voltage on channel 0.
# ///
record(ai, "$(P):SRDG0") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getSRDG(0) $(PORT) $(ADDR)")
  field(SCAN, "$(TEMPSCAN) second")
  field(PREC, "3")
  field(EGU, "V")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the raw voltage on channel 1.
# ///
record(ai, "$(P):SRDG1") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getSRDG(1) $(PORT) $(ADDR)")
  field(SCAN, "$(TEMPSCAN) second")
  field(PREC, "3")
  field(EGU, "V")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the raw voltage on channel 2.
# ///
record(ai, "$(P):SRDG2") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getSRDG(2) $(PORT) $(ADDR)")
  field(SCAN, "$(TEMPSCAN) second")
  field(PREC, "3")
  field(EGU, "V")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the raw voltage on channel 3.
# ///
record(ai, "$(P):SRDG3") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getSRDG(3) $(PORT) $(ADDR)")
  field(SCAN, "$(TEMPSCAN) second")
  field(PREC, "3")
  field(EGU, "V")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the range parameter (the heater output power range).
# ///
record(ai, "$(P):RANGE") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getRANGE $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the ramp paremeter.
# ///
record(ai, "$(P):RAMP") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getRAMP $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(PREC, "3")
  field(EGU, "K/min")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the ramp status parameter.
# .
# /// 0=off
# /// 1=on
# ///
record(ai, "$(P):RAMPST") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getRAMPSTATUS $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the control loop 1 manual output parameter.
# ///
record(ai, "$(P):MOUT") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getMOUT $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the PID P parameter for control loop 1.
# ///
record(ai, "$(P):P") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getP $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(PREC, "1")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the PID I parameter for control loop 1.
# ///
record(ai, "$(P):I") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getI $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(PREC, "1")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the PID D parameter for control loop 1.
# ///
record(ai, "$(P):D") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getD $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(PREC, "0")
  field(SDIS, "$(P):ONOFF")
}

# /// 
# /// Read the control loop 1 mode parameter.
# ///
record(ai, "$(P):CMODE") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getCMODE $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(SDIS, "$(P):ONOFF")
}

# ///
# /// Read the control loop auto tuning status
# ///
record(ai, "$(P):TUNEST") {
  field(DTYP, "stream")
  field(INP, "@lakeshore331.protocol getTUNEST $(PORT) $(ADDR)")
  field(SCAN, "$(SCAN) second")
  field(SDIS, "$(P):ONOFF")
}

################################################################
# Write records
################################################################
# /// 
# /// Set the setpoint temperature.
# ///
#% autosave 1 VAL
record(ao, "$(P):SETP_S") {
  field(DTYP, "stream")
  field(DESC, "Setpoint temperature")
  field(OUT, "@lakeshore331.protocol setSETP $(PORT) $(ADDR)")
  field(PREC, "3")
}

# /// 
# /// Set the range parameter.
# .
# /// Possible values are: 0, 1, 2, 3, 4, 5
# ///
#% autosave 1 VAL
record(ao, "$(P):RANGE_S") {
  field(DTYP, "stream")
  field(OUT, "@lakeshore331.protocol setRANGE $(PORT) $(ADDR)")
}

# /// 
# /// Set the ramp rate parameter.
# .
# /// This is the desired temperate increase/decrease rate
# /// per second when heating/cooling.
# .
# /// The ramp and ramp status parameters are actually one command
# /// for the lakeshore 340. Therefore one must pass into this
# /// record the existing/desired ramp status, as well as the 
# /// desired ramp. This is done automatically by reading the 
# /// PV which holds the current ramp status.
# ///
#% autosave 1 VAL
record(ao, "$(P):RAMP_S") {
  field(DTYP, "stream")
  field(OUT, "@lakeshore331.protocol setRAMP($(P):RAMPST) $(PORT) $(ADDR)")
  field(PREC, "3")
  field(EGU, "K/min")
}

# /// 
# /// Set the ramp status parameter.
# .
# /// 0=off
# /// 1=on
# .
# /// The ramp and ramp status parameters are actually one command
# /// for the lakeshore 340. Therefore one must pass into this
# /// record the existing/desired ramp, as well as the 
# /// desired ramp status. This is done automatically by reading the 
# /// PV which holds the current ramp value.
# ///
#% autosave 1 VAL
record(ao, "$(P):RAMPST_S") {
  field(DTYP, "stream")
  field(OUT, "@lakeshore331.protocol setRAMPSTATUS($(P):RAMP) $(PORT) $(ADDR)")
}

# /// 
# /// Set the control loop 1 manual output value.
# ///
#% autosave 1 VAL
record(ao, "$(P):MOUT_S") {
  field(DTYP, "stream")
  field(OUT, "@lakeshore331.protocol setMOUT $(PORT) $(ADDR)")
}

# ///
# /// Set the PID P parameter for control loop 1.
# ///
#% autosave 1 VAL
record(ao, "$(P):P_S") {
  field(DTYP, "stream")
  field(OUT, "@lakeshore331.protocol setP $(PORT) $(ADDR)")
  field(PREC, "1")
  field(LOPR, "0")
  field(HOPR, "1000")
}

# ///
# /// Set the PID I parameter for control loop 1.
# ///
#% autosave 1 VAL
record(ao, "$(P):I_S") {
  field(DTYP, "stream")
  field(OUT, "@lakeshore331.protocol setI($(P):P) $(PORT) $(ADDR)")
  field(PREC, "1")
  field(LOPR, "0")
  field(HOPR, "1000")
}

# ///
# /// Set the PID D parameter for control loop 1.
# ///
#% autosave 1 VAL
record(ao, "$(P):D_S") {
  field(DTYP, "stream")
  field(OUT, "@lakeshore331.protocol setD($(P):P,$(P):I) $(PORT) $(ADDR)")
  field(PREC, "0")
  field(LOPR, "0")
  field(HOPR, "200")
}

# /// 
# /// Set the control loop 1 mode value.
# ///
#% autosave 1 VAL
record(ao, "$(P):CMODE_S") {
  field(DTYP, "stream")
  field(OUT, "@lakeshore331.protocol setCMODE $(PORT) $(ADDR)")
}

record(bo, "$(P):ONOFF") {
  field(ZNAM, "ON")
  field(ONAM, "OFF")
  field(VAL, "$(ONOFFDEFAULT)")
}

#! Further lines contain data used by VisualDCT
#! View(218,300,1.1)
#! Record("$(P):ID",620,8,0,0,"$(P):ID")
#! Field("$(P):ID.SDIS",16777215,1,"$(P):ID.SDIS")
#! Link("$(P):ID.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):HTR",620,168,0,0,"$(P):HTR")
#! Field("$(P):HTR.SDIS",16777215,1,"$(P):HTR.SDIS")
#! Link("$(P):HTR.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):SETP",620,328,0,0,"$(P):SETP")
#! Field("$(P):SETP.SDIS",16777215,1,"$(P):SETP.SDIS")
#! Link("$(P):SETP.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):KRDG0",620,494,0,0,"$(P):KRDG0")
#! Field("$(P):KRDG0.SDIS",16777215,1,"$(P):KRDG0.SDIS")
#! Link("$(P):KRDG0.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):KRDG1",620,674,0,0,"$(P):KRDG1")
#! Field("$(P):KRDG1.SDIS",16777215,1,"$(P):KRDG1.SDIS")
#! Link("$(P):KRDG1.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):KRDG2",400,694,0,0,"$(P):KRDG2")
#! Field("$(P):KRDG2.SDIS",16777215,1,"$(P):KRDG2.SDIS")
#! Link("$(P):KRDG2.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):KRDG3",400,514,0,0,"$(P):KRDG3")
#! Field("$(P):KRDG3.SDIS",16777215,1,"$(P):KRDG3.SDIS")
#! Link("$(P):KRDG3.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):SRDG0",400,348,0,0,"$(P):SRDG0")
#! Field("$(P):SRDG0.SDIS",16777215,1,"$(P):SRDG0.SDIS")
#! Link("$(P):SRDG0.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):SRDG1",1300,168,0,0,"$(P):SRDG1")
#! Field("$(P):SRDG1.SDIS",16777215,0,"$(P):SRDG1.SDIS")
#! Link("$(P):SRDG1.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):SRDG2",1300,8,0,0,"$(P):SRDG2")
#! Field("$(P):SRDG2.SDIS",16777215,0,"$(P):SRDG2.SDIS")
#! Link("$(P):SRDG2.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):SRDG3",1520,708,0,0,"$(P):SRDG3")
#! Field("$(P):SRDG3.SDIS",16777215,0,"$(P):SRDG3.SDIS")
#! Link("$(P):SRDG3.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):RANGE",1520,528,0,0,"$(P):RANGE")
#! Field("$(P):RANGE.SDIS",16777215,0,"$(P):RANGE.SDIS")
#! Link("$(P):RANGE.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):RAMP",400,188,0,0,"$(P):RAMP")
#! Field("$(P):RAMP.SDIS",16777215,1,"$(P):RAMP.SDIS")
#! Link("$(P):RAMP.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):RAMPST",1520,348,0,0,"$(P):RAMPST")
#! Field("$(P):RAMPST.SDIS",16777215,0,"$(P):RAMPST.SDIS")
#! Link("$(P):RAMPST.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):MOUT",1520,28,0,0,"$(P):MOUT")
#! Field("$(P):MOUT.SDIS",16777215,0,"$(P):MOUT.SDIS")
#! Link("$(P):MOUT.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):P",1300,328,0,0,"$(P):P")
#! Field("$(P):P.SDIS",16777215,0,"$(P):P.SDIS")
#! Link("$(P):P.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):I",1300,488,0,0,"$(P):I")
#! Field("$(P):I.SDIS",16777215,0,"$(P):I.SDIS")
#! Link("$(P):I.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):D",1300,668,0,0,"$(P):D")
#! Field("$(P):D.SDIS",16777215,0,"$(P):D.SDIS")
#! Link("$(P):D.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):CMODE",1520,188,0,0,"$(P):CMODE")
#! Field("$(P):CMODE.SDIS",16777215,0,"$(P):CMODE.SDIS")
#! Link("$(P):CMODE.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):TUNEST",400,28,0,0,"$(P):TUNEST")
#! Field("$(P):TUNEST.SDIS",16777215,1,"$(P):TUNEST.SDIS")
#! Link("$(P):TUNEST.SDIS","$(P):ONOFF.VAL")
#! Record("$(P):SETP_S",60,1521,0,0,"$(P):SETP_S")
#! Record("$(P):RANGE_S",300,1516,0,0,"$(P):RANGE_S")
#! Record("$(P):RAMP_S",540,1516,0,0,"$(P):RAMP_S")
#! Record("$(P):RAMPST_S",780,1516,0,0,"$(P):RAMPST_S")
#! Record("$(P):MOUT_S",1020,1516,0,0,"$(P):MOUT_S")
#! Record("$(P):P_S",1260,1516,0,0,"$(P):P_S")
#! Record("$(P):I_S",1500,1516,0,0,"$(P):I_S")
#! Record("$(P):D_S",1740,1516,0,0,"$(P):D_S")
#! Record("$(P):CMODE_S",1980,1516,0,0,"$(P):CMODE_S")
#! Record("$(P):ONOFF",940,381,0,1,"$(P):ONOFF")
#! Field("$(P):ONOFF.VAL",16777215,1,"$(P):ONOFF.VAL")
